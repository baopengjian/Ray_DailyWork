<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.baopengjian.ray_dailywork">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2018-5 -->
        <activity android:name=".may.MayActivity"/>
        <activity android:name=".may.ListHorizontalScrollActivity"/>

        <activity android:name=".april.ButtonShadowActivity"
            />
        <activity android:name=".april.PartiallyClickableActivity"/>
        <activity android:name=".april.AprilActivity"/>
        <activity android:name=".april.LoadPdfActivity"/>
        <activity android:name=".april.DependsActivity"/>
        <activity android:name=".april.ShadowsActivity"/>
        <activity android:name=".may.bar.BarChartViewActivity"/>
    </application>

</manifest>